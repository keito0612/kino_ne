import 'package:flutter/material.dart';

class AppColors {
  static const bgColor = Color(0xFFF2F0E4); // 背景のベージュ
  static const primaryGreen = Colors.green; // メインのグリーン
  static const accentGold = Color(0xFFD4AF37); // レベルアップ等のアクセント
  static const cardColor = Colors.white;
  static const textDark = Color(0xFF4A4A4A);
}
